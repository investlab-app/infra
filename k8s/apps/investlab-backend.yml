apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: investlab-backend
  namespace: argocd
  labels:
    app.kubernetes.io/name: investlab-backend
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: investlab
spec:
  project: default
  source:
    repoURL: https://github.com/investlab-app/infra.git
    targetRevision: rewrite
    path: charts/investlab-backend
    helm:
      valueFiles:
        - values.yaml
        - values-database.yaml
        - values-redis.yaml
        - values-monitoring.yaml
        - values-networkPolicy.yaml
      releaseName: investlab-backend
  destination:
    server: https://kubernetes.default.svc
    namespace: backend
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
