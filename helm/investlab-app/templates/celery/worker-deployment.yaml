{{- if .Values.celery.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "investlab-app.fullname" . }}-celery-worker
  labels:
    {{- include "investlab-app.labels" . | nindent 4 }}
    app.kubernetes.io/component: celery-worker
spec:
  replicas: {{ .Values.celery.worker.replicaCount }}
  selector:
    matchLabels:
      {{- include "investlab-app.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: celery-worker
  template:
    metadata:
      labels:
        {{- include "investlab-app.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: celery-worker
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "investlab-app.serviceAccountName" . }}
      containers:
        - name: celery-worker
          image: {{ include "investlab-app.backendImage" . }}
          imagePullPolicy: {{ .Values.backend.image.pullPolicy }}
          command: ["uv", "run", "celery", "-A", "config", "worker", "-l", "info"]
          env:
            {{- include "investlab-app.backendEnvVars" . | nindent 12 }}
          resources:
            {{- toYaml .Values.celery.worker.resources | nindent 12 }}
{{- end }}